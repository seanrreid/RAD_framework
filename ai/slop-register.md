# Project Slop Register

Use this file for repo-specific mistakes coding agents commonly make. Keep it short, current, and concrete.

Until this file is customized for a repository, the sections below define no additional active rules beyond `ai/guardrails.md`.

## Deprecated Or Forbidden

- None configured yet.

Examples:

- Do not use: `[deprecated library/API/pattern]`.

## Required Conventions

- None configured yet.

Examples:

- Always use: `[local helper/convention]` for `[case]`.

## Layering Rules

- None configured yet.

Examples:

- Never place: `[responsibility]` in `[wrong module/layer]`.

## Repeat Failure Modes

- None configured yet.

Examples:

- Watch for: `[repeated AI mistake]` because `[failure mode]`.

## Required Checks

- None configured yet.
- If no project-specific checks are listed here and no local tool manifest exists, report that no deterministic checks are configured instead of inventing commands.

Examples:

- Run before handoff: `[commands or CI jobs]`.

## Notes

- Add an item when the same agent mistake appears more than once.
- Remove or update stale items when the codebase changes.
