# Logs Directory

Execution logs created by `/rad-deliver`. One file per plan execution.

## Naming

```
[feature-name]-[YYYY-MM-DD].md
```

Logs are the resume state for interrupted executions and the audit trail
for architect code review. Do not delete them until the deliver PR is merged.
